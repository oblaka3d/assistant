# Unity WebGL Build для голосового ассистента

Этот каталог предназначен для размещения Unity WebGL билда персонажа.

> 📋 **Техническое задание для аниматора!** Если вы работаете с 3D аниматором, предоставьте техническое задание: **[TECHNICAL_SPECIFICATION.md](./TECHNICAL_SPECIFICATION.md)**  
> 📘 **Подробная инструкция!** Если вы создаете Unity проект с нуля, следуйте пошаговой инструкции: **[SETUP_GUIDE.md](./SETUP_GUIDE.md)**

## 📁 Структура каталога

После сборки Unity WebGL проекта, структура должна выглядеть следующим образом:

```
/unity
  /Build
    build.loader.js      # Unity Loader скрипт
    build.framework.js   # Unity Framework
    build.data          # Данные сцены
    build.wasm          # WebAssembly модуль
  /Template
    index.html          # HTML шаблон (опционально)
```

## 🎮 Требования к Unity проекту

### Настройки билда

1. **Platform**: WebGL
2. **Compression Format**: Gzip (рекомендуется) или Brotli
3. **Code Optimization**: Size
4. **Exception Support**: None (для лучшей производительности)

### Необходимые компоненты в Unity

#### GameObject "CharacterController"

Создайте GameObject с именем `CharacterController` и добавьте скрипт для управления анимациями:

```csharp
using UnityEngine;

public class CharacterController : MonoBehaviour
{
    private Animator animator;
    
    void Start()
    {
        animator = GetComponent<Animator>();
    }
    
    public void PlayIdle()
    {
        animator.SetTrigger("Idle");
    }
    
    public void PlayListening()
    {
        animator.SetTrigger("Listening");
    }
    
    public void PlayThinking()
    {
        animator.SetTrigger("Thinking");
    }
    
    public void PlayTalking()
    {
        animator.SetTrigger("Talking");
    }
}
```

#### Анимации

Убедитесь, что в Animator Controller есть следующие триггеры:
- `Idle` - анимация покоя
- `Listening` - анимация прослушивания
- `Thinking` - анимация размышления
- `Talking` - анимация разговора

### Toon Shader

Используйте Unity Toon Shader (UTS) или аналогичный для стилизации персонажа:
- Unity Toon Shader (URP/HDRP)
- Amplify Shader Editor с Toon эффектами
- Custom Toon Shader

### Настройки камеры

- **Projection**: Perspective
- **Field of View**: 50-60 градусов
- **Background**: Solid Color (черный или прозрачный)

## 🔧 Интеграция с Electron

### Отправка сообщений из Unity в JavaScript

В Unity скрипте можно отправлять сообщения обратно в Electron:

```csharp
using UnityEngine;
using System.Runtime.InteropServices;

public class UnityToJS : MonoBehaviour
{
    [DllImport("__Internal")]
    private static extern void unityReceive(string type, string data);
    
    public void OnAnimationComplete(string animationName)
    {
        unityReceive("animation", animationName);
    }
}
```

### JavaScript функция для Unity

В `unity-wrapper.ts` уже настроена функция `window.unityReceive`, которая обрабатывает сообщения от Unity.

## 📦 Сборка Unity проекта

1. Откройте проект в Unity
2. File → Build Settings
3. Выберите WebGL платформу
4. Настройте Player Settings:
   - **Company Name**: VoiceAssistant
   - **Product Name**: Voice Assistant ARM
   - **Default Canvas Scaler**: Scale With Screen Size
5. Build → выберите каталог `unity/Build`
6. После сборки скопируйте файлы в структуру, указанную выше

## 🚀 Проверка билда

После размещения файлов в каталоге `/unity`, при запуске Electron приложения:

1. Unity WebGL должен загрузиться автоматически
2. Персонаж должен отображаться в центре экрана
3. Анимации должны переключаться при взаимодействии с голосовым ассистентом

## ⚠️ Важные замечания

- **Размер билда**: Старайтесь минимизировать размер билда для быстрой загрузки на ARM устройствах
- **Производительность**: Оптимизируйте полигоны, текстуры и шейдеры для ARM GPU
- **Совместимость**: Убедитесь, что WebGL билд совместим с Electron (Chromium)
- **Пути**: Все пути к ресурсам должны быть относительными

## 🐛 Решение проблем

### Unity не загружается

- Проверьте пути к файлам в `unity-loader.ts`
- Убедитесь, что все файлы билда присутствуют
- Проверьте консоль браузера на ошибки

### Анимации не работают

- Убедитесь, что GameObject называется `CharacterController`
- Проверьте, что методы `PlayIdle`, `PlayListening`, и т.д. публичные
- Проверьте настройки Animator Controller

### Низкая производительность

- Уменьшите разрешение рендеринга в Unity
- Оптимизируйте количество полигонов
- Используйте более простые шейдеры
- Включите GPU instancing где возможно

