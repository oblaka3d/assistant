# 📘 Подробная инструкция по созданию Unity проекта для голосового ассистента

Эта инструкция поможет вам создать Unity проект с нуля для интеграции с голосовым ассистентом.

## 📋 Требования

- **Unity Editor** версии 2021.3 LTS или новее (рекомендуется 2022.3 LTS)
- **WebGL Build Support** модуль (устанавливается через Unity Hub)
- Базовые знания Unity (работа со сценами, GameObject, компонентами)

---

## 🚀 Шаг 1: Создание нового Unity проекта

1. Откройте **Unity Hub**
2. Нажмите **"New Project"**
3. Выберите шаблон **"3D (URP)"** или **"3D Core"**
   - URP (Universal Render Pipeline) рекомендуется для лучшей производительности
   - Если используете URP, будет проще работать с Toon Shader
4. Укажите:
   - **Project name**: `VoiceAssistantCharacter` (или любое другое название)
   - **Location**: выберите папку для проекта
   - **Unity version**: 2021.3 LTS или 2022.3 LTS
5. Нажмите **"Create project"**

---

## 🎨 Шаг 2: Подготовка сцены

### 2.1 Очистка сцены

1. В иерархии удалите объект **"Main Camera"** (мы создадим новый)
2. Оставьте только **"Directional Light"** (или настройте освещение по необходимости)

### 2.2 Создание камеры

1. **Hierarchy** → правый клик → **Camera**
2. Переименуйте камеру в **"MainCamera"**
3. Настройте камеру:
   - **Transform Position**: `(0, 1.5, -5)` - для вида спереди
   - **Transform Rotation**: `(15, 0, 0)` - слегка сверху
   - **Camera** → **Projection**: `Perspective`
   - **Camera** → **Field of View**: `55`
   - **Camera** → **Background** → **Solid Color**: черный `#000000` или прозрачный
   - **Camera** → **Clear Flags**: `Solid Color` или `Skybox`

---

## 👤 Шаг 3: Создание или импорт персонажа

### Вариант A: Использование готового персонажа (рекомендуется)

1. Импортируйте 3D модель персонажа (FBX, OBJ и т.д.)
2. Перетащите модель в **Hierarchy**
3. Разместите персонажа в центре сцены: `Position (0, 0, 0)`
4. При необходимости масштабируйте модель

### Вариант B: Простой примитивный персонаж для тестирования

1. **Hierarchy** → правый клик → **3D Object** → **Capsule**
2. Переименуйте в **"Character"**
3. **Position**: `(0, 1, 0)` - чтобы был на земле
4. Добавьте материал и назначьте ему Toon Shader (см. Шаг 5)

---

## 🎭 Шаг 4: Настройка анимаций и Animator

### 4.1 Создание Animator Controller

1. **Project** → правый клик в папке `Assets` → **Create** → **Animator Controller**
2. Назовите его **"CharacterAnimator"**
3. Дважды кликните, чтобы открыть в **Animator** окне

### 4.2 Настройка состояний анимации

В окне **Animator**:

1. Создайте 4 состояния (клик правой кнопкой → **Create State** → **Empty**):
   - **Idle** - анимация покоя
   - **Listening** - анимация прослушивания
   - **Thinking** - анимация размышления
   - **Talking** - анимация разговора

2. Для каждого состояния:
   - **Motion**: назначьте анимацию (или оставьте None для заглушки)
   - Если нет готовых анимаций, создайте простые (см. Шаг 4.3)

3. Настройте переходы между состояниями:
   - Каждое состояние должно иметь переход в каждое другое
   - Все переходы должны использовать **Triggers** (не параметры Bool)

### 4.3 Создание триггеров

1. В **Animator** окне откройте вкладку **Parameters** (слева внизу)
2. Нажмите **"+"** → **Trigger**
3. Создайте 4 триггера (имена должны быть точно такими):
   - `Idle`
   - `Listening`
   - `Thinking`
   - `Talking`

4. Настройте переходы:
   - Выберите состояние **Idle** → **Add Transition** → выберите **Listening**
   - В переходе выберите **Conditions** → добавьте условие **Listening** (Trigger)
   - Повторите для всех комбинаций состояний

### 4.4 Настройка переходов (важно!)

Для каждого перехода:
- **Has Exit Time**: ❌ **снимите галочку** (чтобы переход был мгновенным)
- **Transition Duration**: `0` (мгновенный переход)
- **Condition**: выберите соответствующий триггер

**Пример настройки переходов:**
```
Idle → Listening (Trigger: Listening)
Idle → Thinking (Trigger: Thinking)
Idle → Talking (Trigger: Talking)

Listening → Idle (Trigger: Idle)
Listening → Thinking (Trigger: Thinking)
Listening → Talking (Trigger: Talking)

... и так далее для всех комбинаций
```

### 4.5 Установка Entry State

1. Кликните правой кнопкой на состояние **Idle**
2. Выберите **"Set as Layer Default State"** (должно стать оранжевым)

---

## 💻 Шаг 5: Создание скрипта CharacterController

### 5.1 Создание скрипта

1. **Project** → правый клик → **Create** → **C# Script**
2. Назовите скрипт **"CharacterController"** (точное название!)
3. Дважды кликните, чтобы открыть в редакторе кода

### 5.2 Код скрипта

Замените содержимое скрипта на следующий код:

```csharp
using UnityEngine;

public class CharacterController : MonoBehaviour
{
    private Animator animator;
    
    void Start()
    {
        // Получаем компонент Animator
        animator = GetComponent<Animator>();
        
        // Проверяем наличие Animator
        if (animator == null)
        {
            Debug.LogError("CharacterController: Animator component not found!");
        }
        
        // Запускаем начальную анимацию Idle
        PlayIdle();
    }
    
    /// <summary>
    /// Воспроизводит анимацию покоя (Idle)
    /// </summary>
    public void PlayIdle()
    {
        if (animator != null)
        {
            animator.SetTrigger("Idle");
            Debug.Log("CharacterController: Playing Idle animation");
        }
    }
    
    /// <summary>
    /// Воспроизводит анимацию прослушивания (Listening)
    /// </summary>
    public void PlayListening()
    {
        if (animator != null)
        {
            animator.SetTrigger("Listening");
            Debug.Log("CharacterController: Playing Listening animation");
        }
    }
    
    /// <summary>
    /// Воспроизводит анимацию размышления (Thinking)
    /// </summary>
    public void PlayThinking()
    {
        if (animator != null)
        {
            animator.SetTrigger("Thinking");
            Debug.Log("CharacterController: Playing Thinking animation");
        }
    }
    
    /// <summary>
    /// Воспроизводит анимацию разговора (Talking)
    /// </summary>
    public void PlayTalking()
    {
        if (animator != null)
        {
            animator.SetTrigger("Talking");
            Debug.Log("CharacterController: Playing Talking animation");
        }
    }
}
```

### 5.3 Сохранение и компиляция

1. Сохраните файл (`Ctrl+S` или `Cmd+S`)
2. Вернитесь в Unity Editor
3. Дождитесь автоматической компиляции (индикатор в правом нижнем углу)

---

## 🎬 Шаг 6: Привязка компонентов к персонажу

### 6.1 Переименование GameObject

1. В **Hierarchy** выберите ваш персонаж (например, "Character")
2. Переименуйте GameObject в **"CharacterController"** (точное название важно!)

### 6.2 Добавление компонентов

Выберите **CharacterController** в Hierarchy и на **Inspector** добавьте:

1. **Animator**:
   - Нажмите **"Add Component"** → **Animator**
   - В поле **Controller** перетащите созданный ранее **CharacterAnimator**

2. **CharacterController Script**:
   - Нажмите **"Add Component"** → найдите и добавьте **"Character Controller (Script)"**
   - Или перетащите скрипт **CharacterController.cs** на GameObject

---

## 🎨 Шаг 7: Настройка материалов и Toon Shader

### Вариант A: Unity Toon Shader (URP)

Если вы используете **URP** (Universal Render Pipeline):

1. Установите **Unity Toon Shader** через Package Manager:
   - **Window** → **Package Manager**
   - Нажмите **"+"** → **"Add package from git URL"**
   - Введите: `https://github.com/unity3d-jp/UnityChanToonShaderVer2_Project.git?path=/UTS2`

2. Создайте материал:
   - **Project** → правый клик → **Create** → **Material**
   - Назовите **"CharacterMaterial"**
   - В **Inspector** выберите **Shader** → **UnityChanToonShader 2.0** → выберите подходящий вариант

3. Назначьте материал персонажу

### Вариант B: Простой Shader (для тестирования)

1. **Project** → правый клик → **Create** → **Material**
2. Назовите **"CharacterMaterial"**
3. Настройте:
   - **Albedo**: выберите цвет или текстуру
   - **Metallic**: `0`
   - **Smoothness**: `0.3` (для более "мультяшного" вида)
4. Назначьте материал персонажу (перетащите на GameObject)

### Вариант C: Использование готового Toon Shader из Asset Store

1. **Window** → **Asset Store**
2. Найдите **"Toon Shader"** или **"Anime Shader"**
3. Импортируйте в проект
4. Используйте в материалах персонажа

---

## 📦 Шаг 8: Настройка билда для WebGL

### 8.1 Добавление WebGL платформы

1. **File** → **Build Settings...**
2. В списке **Platform** выберите **"WebGL"**
3. Если платформа не установлена:
   - Нажмите **"Open Download Page"**
   - Установите **"WebGL Build Support"** через Unity Hub
   - После установки вернитесь и выберите **"Switch Platform"**

### 8.2 Настройка Player Settings

1. В окне **Build Settings** нажмите **"Player Settings..."**
2. В **Inspector** откройте раздел **"Player"**

   **Вкладка "Other Settings":**
   - **Company Name**: `VoiceAssistant`
   - **Product Name**: `Voice Assistant ARM`
   - **Version**: `1.0.0`
   - **Scripting Backend**: `Mono` или `IL2CPP` (IL2CPP для лучшей производительности)
   - **Api Compatibility Level**: `.NET Standard 2.1`

   **Вкладка "Publishing Settings":**
   - **Compression Format**: `Gzip` (рекомендуется) или `Brotli`
   - **Data caching**: ✅ включите (для ускорения загрузки)

   **Вкладка "Resolution and Presentation":**
   - **Default Canvas Scaler**: `Scale With Screen Size`
   - **Run In Background**: ✅ включите (если нужно)

### 8.3 Дополнительные настройки для оптимизации

В **Player Settings** → **Other Settings**:

- **Code Optimization**: `Size` (для уменьшения размера билда)
- **Exception Support**: `None` (для лучшей производительности)
- **Strip Engine Code**: ✅ включите (удалит неиспользуемый код)

---

## 🔧 Шаг 9: (Опционально) Интеграция обратной связи с JavaScript

Если вы хотите отправлять сообщения из Unity в JavaScript:

1. Создайте новый скрипт **UnityToJS.cs**:

```csharp
using UnityEngine;
using System.Runtime.InteropServices;

public class UnityToJS : MonoBehaviour
{
    [DllImport("__Internal")]
    private static extern void unityReceive(string type, string data);
    
    /// <summary>
    /// Отправляет сообщение в JavaScript (например, когда анимация завершена)
    /// </summary>
    public void OnAnimationComplete(string animationName)
    {
        #if UNITY_WEBGL && !UNITY_EDITOR
        unityReceive("animation", animationName);
        #endif
    }
}
```

2. Добавьте этот скрипт на GameObject **CharacterController**

**Примечание**: Эта функциональность опциональна. Базовый функционал будет работать без неё.

---

## 🏗️ Шаг 10: Сборка проекта

### 10.1 Подготовка папки Build

1. В файловом менеджере откройте папку проекта вашего Electron приложения
2. Убедитесь, что папка `unity/Build/` существует (должна быть создана автоматически)

### 10.2 Сборка в Unity

1. **File** → **Build Settings...**
2. Убедитесь, что выбрана платформа **"WebGL"**
3. Нажмите **"Build"**
4. Выберите папку: `[путь_к_проекту]/unity/Build`
5. Дождитесь завершения сборки

### 10.3 Проверка файлов билда

После сборки в папке `unity/Build/` должны быть следующие файлы:

- `Build/WebGL.loader.js` (или похожий, зависит от версии Unity)
- `Build/WebGL.framework.js` (или `build.framework.js`)
- `Build/WebGL.data` (или `build.data`)
- `Build/WebGL.wasm` (или `build.wasm`)

**Важно**: Unity может генерировать файлы с разными именами в зависимости от версии. Проверьте, какие именно файлы были созданы.

### 10.4 Переименование файлов (если нужно)

Если файлы имеют другие имена, переименуйте их в соответствии с ожидаемыми именами:

- `build.loader.js`
- `build.framework.js`
- `build.data`
- `build.wasm`

Или обновите пути в `app/ui/unity-loader.ts` под ваши имена файлов.

---

## ✅ Шаг 11: Проверка сборки

### 11.1 Локальная проверка (через Unity)

1. В **Build Settings** нажмите **"Build And Run"**
2. Unity откроет браузер с вашим билдом
3. Убедитесь, что персонаж отображается

### 11.2 Проверка в Electron приложении

1. Убедитесь, что файлы билда находятся в `unity/Build/`
2. Соберите Electron проект: `npm run build`
3. Запустите Electron: `npm run electron:window`
4. Проверьте консоль (DevTools) на наличие ошибок

---

## 🐛 Частые проблемы и решения

### Проблема 1: GameObject не найден

**Симптом**: В консоли ошибка "GameObject CharacterController not found"

**Решение**:
- Убедитесь, что GameObject в сцене называется точно **"CharacterController"** (без пробелов, с заглавными буквами)
- GameObject должен быть активен в сцене
- Проверьте, что сцена сохранена

### Проблема 2: Методы не вызываются

**Симптом**: Анимации не переключаются

**Решение**:
- Проверьте, что скрипт **CharacterController** добавлен на GameObject
- Убедитесь, что методы **public** (не private)
- Проверьте, что Animator Controller назначен правильно
- Откройте консоль Unity и проверьте, вызываются ли методы (должны быть логи Debug.Log)

### Проблема 3: Триггеры не работают

**Симптом**: Анимации не переключаются на правильные состояния

**Решение**:
- Проверьте, что триггеры созданы с правильными именами (Idle, Listening, Thinking, Talking)
- Убедитесь, что переходы настроены с правильными триггерами
- Проверьте, что **Has Exit Time** отключен для переходов
- Убедитесь, что **Transition Duration** = 0

### Проблема 4: Билд не загружается в Electron

**Симптом**: Unity не загружается, ошибки в консоли

**Решение**:
- Проверьте пути к файлам в `unity-loader.ts`
- Убедитесь, что все файлы билда присутствуют
- Проверьте имена файлов (Unity может генерировать другие имена)
- Откройте DevTools и проверьте Network tab на наличие 404 ошибок

### Проблема 5: Персонаж не виден

**Симптом**: Экран черный, персонаж не отображается

**Решение**:
- Проверьте настройки камеры (Position, Rotation, Field of View)
- Убедитесь, что персонаж находится в поле зрения камеры
- Проверьте освещение сцены
- Убедитесь, что материалы назначены правильно

### Проблема 6: Ошибки компиляции скрипта

**Симптом**: Скрипт не компилируется в Unity

**Решение**:
- Проверьте синтаксис C# (точки с запятой, скобки)
- Убедитесь, что используется правильная версия Unity
- Проверьте, что нет конфликтов имен классов
- Откройте **Console** в Unity и проверьте ошибки компиляции

---

## 📚 Дополнительные ресурсы

### Рекомендуемые версии Unity

- **Unity 2021.3 LTS** - стабильная версия
- **Unity 2022.3 LTS** - более новая стабильная версия

### Полезные ссылки

- [Unity WebGL документация](https://docs.unity3d.com/Manual/webgl.html)
- [Unity Animator документация](https://docs.unity3d.com/Manual/class-AnimatorController.html)
- [Unity Toon Shader (GitHub)](https://github.com/unity3d-jp/UnityChanToonShaderVer2_Project)

---

## ✅ Чек-лист перед сборкой

Перед финальной сборкой убедитесь:

- [ ] GameObject называется точно **"CharacterController"**
- [ ] Скрипт **CharacterController.cs** добавлен на GameObject
- [ ] Animator Controller назначен на персонаже
- [ ] Все 4 триггера созданы (Idle, Listening, Thinking, Talking)
- [ ] Переходы настроены между всеми состояниями
- [ ] Методы PlayIdle(), PlayListening(), PlayThinking(), PlayTalking() **public**
- [ ] Камера настроена правильно
- [ ] Player Settings настроены (Company Name, Product Name)
- [ ] WebGL платформа выбрана в Build Settings
- [ ] Сцена сохранена

---

## 🎉 Готово!

После выполнения всех шагов ваш Unity проект готов к интеграции с голосовым ассистентом. 

При запуске Electron приложения персонаж должен автоматически загрузиться, и анимации должны переключаться в зависимости от состояния голосового ассистента:
- **Idle** - когда ассистент готов к работе
- **Listening** - во время записи голоса
- **Thinking** - во время обработки запроса
- **Talking** - во время воспроизведения ответа

Удачи! 🚀

