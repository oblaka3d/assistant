# 🎤 Голосовой Ассистент с 3D Персонажем

Полнофункциональный голосовой ассистент на Electron + React + THREE.js с 3D визуализацией персонажа.

## 🎯 Основные возможности

- 🎙️ Голосовое взаимодействие (запись, распознавание речи, синтез речи)
- 🤖 Интеграция с LLM (OpenAI, Yandex GPT)
- 👤 3D персонаж на THREE.js с анимациями
- 💬 Текстовый чат с историей сообщений
- ⚙️ Настройки модели и сцены
- 🔄 Круговая навигация между экранами (свайпы)
- 🔐 Безопасное хранение API ключей (шифрование на сервере)

## 📋 Требования

- **Node.js**: 18+
- **ОС**: Linux, macOS, Windows
- **Аудио**: `sox` или `arecord`/`aplay` (Linux)

## 🚀 Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd assistant

# Установка зависимостей
npm install

# Сборка проекта
npm run build

# Запуск приложения
npm start
```

## 📁 Структура проекта

```
assistant/
├── main/                  # Electron main процесс
├── backend-electron/      # Backend логика (STT, LLM, TTS)
├── ui-electron/           # React UI приложение
│   ├── src/
│   │   ├── screens/      # Экраны приложения
│   │   ├── components/   # React компоненты
│   │   ├── renderer/     # THREE.js рендерер
│   │   └── store/        # Redux store
│   └── public/
│       └── assets/
│           ├── models/   # GLB модели персонажей
│           └── scenes/   # Файлы сцен окружения
├── backend-main/          # Основной бэкенд сервер (Node.js + MongoDB)
├── docs/                  # Документация проекта
├── dist/                  # Собранные файлы
└── package.json
```

## 🎮 Экраны приложения

- **Main Screen** - главный экран с 3D персонажем и голосовым управлением
- **Chat Screen** - текстовый чат с историей сообщений
- **Menu Screen** - меню с настройками, историей и информацией

## ⚙️ Настройки

В меню → Настройки → Модель и сцена:

- Выбор модели персонажа из списка (GLB файлы из `assets/models/`)
- Выбор сцены окружения (JSON/GLTF файлы из `assets/scenes/`)
- Toon Shader (вкл/выкл)
- Интенсивность освещения
- Расстояние камеры
- Скорость анимации

## 🎨 3D Персонаж

Персонаж загружается из GLB файлов и поддерживает:

- Встроенные анимации из GLB (idle, talking, listening, thinking)
- Программные анимации (если нет встроенных)
- T-pose по умолчанию
- Плавные переходы между анимациями

## 🛠️ Разработка

### Запуск всего проекта

**Автоматическая установка и настройка MongoDB:**
Команда `npm run run:all` автоматически проверит наличие MongoDB, установит её при необходимости, запустит и инициализирует базу данных.

**Ручная настройка MongoDB (опционально):**

```bash
# Автоматическая установка и настройка (с подтверждением)
npm run setup:mongodb

# Автоматическая установка и настройка (без подтверждения)
npm run setup:mongodb:auto

# Ручной запуск MongoDB:
# macOS (Homebrew)
brew services start mongodb-community

# Linux
sudo systemctl start mongod

# Или вручную
mongod --dbpath /path/to/data
```

**Полный запуск проекта:**

```bash
# Полный запуск всего проекта:
# 1. Автоматически проверяет и устанавливает MongoDB (если нужно)
# 2. Билдит backend-main, Electron main и UI
# 3. Автоматически создает БД MongoDB при первом подключении (если не существует)
# 4. Запускает backend-main сервер (порт 3001) - таймаут подключения к MongoDB: 5 секунд
# 5. Запускает Electron приложение в оконном режиме
npm run run:all
```

**Поддерживаемые платформы для автоматической установки:**

- ✅ macOS (через Homebrew)
- ✅ Linux Ubuntu/Debian (через apt)
- ⚠️ Windows (требует ручной установки)

### Другие команды

```bash
# Разработка UI (hot reload)
npm run dev:ui

# Сборка только UI
npm run build:ui

# Сборка backend-main сервера
npm run build:backend-main

# Сборка всего проекта (Electron main + UI)
npm run build

# Сборка всего проекта включая backend-main
npm run build:all

# Запуск backend-main сервера отдельно
npm run start:backend-main

# Запуск backend-main в режиме разработки (hot reload)
npm run dev:backend-main

# Запуск Electron в оконном режиме
npm run start:window

# Запуск с DevTools
npm start
```

### Переменные окружения

Обязательно задайте секрет для шифрования API ключей (используется `backend-main`):

```
API_KEY_SECRET=<любой_достаточно_длинный_секрет>
```

Без него сервер не сможет сохранить ключи.

## 📦 Технологии

- **Electron** - десктопное приложение
- **React** - UI фреймворк
- **Redux Toolkit** - управление состоянием
- **THREE.js** - 3D графика
- **Material-UI** - компоненты интерфейса
- **TypeScript** - типизация

## 📝 Лицензия

MIT
