# 🎤 Голосовой Ассистент для ARM устройств

Полнофункциональный голосовой ассистент на Electron, Node.js и TypeScript, оптимизированный для запуска на ARM-процессорах (Allwinner, Rockchip, Broadcom) под Linux (Armbian, Debian, Ubuntu).

> 📋 **План реализации проекта!** Если вы начинаете проект с нуля, следуйте пошаговому плану: **[PROJECT_PLAN.md](./PROJECT_PLAN.md)**

## 📋 Требования

- **ОС**: Linux (Armbian / Debian / Ubuntu)
- **Архитектура**: ARM64 (ARMv8)
- **Node.js**: 18+ 
- **ALSA**: Установленные утилиты `arecord` и `aplay`

## 🚀 Быстрый старт

### 1. Установка зависимостей системы

```bash
# Установка ALSA утилит
sudo apt update
sudo apt install -y alsa-utils alsa-base

# Проверка доступных аудио устройств
arecord -l
aplay -l

# Настройка микрофона (если нужно)
alsamixer
```

### 2. Установка зависимостей проекта

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта (опционально):

```env
# STT API (OpenAI Whisper, Yandex SpeechKit и т.д.)
STT_API_URL=https://api.openai.com/v1/audio/transcriptions
STT_API_KEY=your_stt_api_key

# LLM API (OpenAI, DeepSeek, YandexGPT и т.д.)
LLM_API_URL=https://api.openai.com/v1/chat/completions
LLM_API_KEY=your_llm_api_key
LLM_MODEL=gpt-3.5-turbo

# TTS API (Yandex TTS, Silero TTS и т.д.)
TTS_API_URL=https://tts.api.cloud.yandex.net/speech/v1/tts:synthesize
TTS_API_KEY=your_tts_api_key
TTS_VOICE=jane
```

**Примечание**: Если API ключи не настроены, приложение будет работать в режиме заглушек (mock mode) для тестирования.

### 4. Установка зависимостей UI

UI приложение построено на React. Установите зависимости:

```bash
npm install
```

### 5. Подготовка Unity WebGL билда

Поместите Unity WebGL билд в каталог `/unity`:

```bash
# Структура должна быть:
unity/
  Build/
    build.loader.js
    build.framework.js
    build.data
    build.wasm
```

**📘 Подробная инструкция по созданию Unity проекта:**
- **Работаете с аниматором?** → Предоставьте техническое задание: **[unity/TECHNICAL_SPECIFICATION.md](./unity/TECHNICAL_SPECIFICATION.md)**
- **Есть готовая модель и сцена?** → Предоставьте ТЗ разработчику: **[unity/DEVELOPER_SPECIFICATION.md](./unity/DEVELOPER_SPECIFICATION.md)**
- **Создаете проект с нуля?** → Следуйте пошаговой инструкции: **[unity/SETUP_GUIDE.md](./unity/SETUP_GUIDE.md)**
- **Уже есть проект?** → Смотрите требования и настройки: **[unity/README.md](./unity/README.md)**

**Примечание**: Если Unity билд отсутствует, приложение все равно запустится, но персонаж не будет отображаться.

### 6. Сборка проекта

```bash
# Компиляция TypeScript и сборка React UI
npm run build
```

Или отдельно:

```bash
# Только TypeScript
tsc

# Только React UI
npm run build:ui
```

### 7. Запуск

```bash
# Запуск в полноэкранном режиме (режим разработки)
npm run dev

# Запуск в оконном режиме (режим разработки)
npm run dev:window

# Запуск скомпилированного приложения (полноэкранный режим)
npm run electron

# Запуск скомпилированного приложения (оконный режим)
npm run electron:window
```

## 📱 Интерфейс

Приложение имеет три экрана с навигацией через свайпы:

1. **Главный экран** - Unity персонаж в центре, управление голосом
2. **Экран чата** (свайп влево) - история текстовых диалогов с ассистентом
3. **Экран меню** (свайп вправо) - настройки и дополнительная информация

### Навигация

- **Свайп влево** с главного экрана → открыть чат
- **Свайп вправо** с главного экрана → открыть меню
- **Свайп влево/вправо** с чата/меню → вернуться на главный экран

## 🛠️ Разработка

### Структура проекта

```
/app
  /main          # Electron main процесс
    electron.ts  # Точка входа Electron
    ipc.ts       # IPC handlers
    preload.ts   # Preload скрипт
  /backend       # Backend модули
    server.ts    # Express сервер (опционально)
    voice.ts     # Захват аудио через arecord
    stt.ts       # Speech-to-Text
    llm.ts       # LLM API интеграция
    tts.ts       # Text-to-Speech
    config.ts    # Конфигурация
    audio-utils.ts # Утилиты для работы с аудио
  /ui            # Пользовательский интерфейс (React)
    index.html   # HTML разметка
    src/         # React исходники
      main.tsx   # Точка входа React
      App.tsx    # Главный компонент с навигацией
      screens/   # Экраны приложения
        MainScreen.tsx    # Главный экран с Unity персонажем
        ChatScreen.tsx    # Экран текстового чата
        MenuScreen.tsx    # Экран меню и настроек
      types/     # TypeScript типы
    unity-loader.ts  # Unity WebGL загрузчик
    unity-wrapper.ts # Unity обертка для анимаций
/unity           # Unity WebGL билд
  /Build         # Файлы Unity билда
    build.loader.js
    build.framework.js
    build.data
    build.wasm
  /Template      # HTML шаблон (опционально)
/types
    global.d.ts  # Глобальные типы TypeScript
```

### Скрипты

- `npm start` - Запуск через ts-node (для разработки, полноэкранный режим)
- `npm run start:window` - Запуск через ts-node (для разработки, оконный режим)
- `npm run dev` - Запуск с watch mode и автоматической перезагрузкой (полноэкранный режим)
- `npm run dev:window` - Запуск с watch mode и автоматической перезагрузкой (оконный режим)
- `npm run build` - Компиляция TypeScript в JavaScript
- `npm run electron` - Запуск скомпилированного Electron приложения (полноэкранный режим)
- `npm run electron:window` - Запуск скомпилированного Electron приложения (оконный режим)
- `npm run dist` - Сборка дистрибутива для Linux ARM64

## 🎯 Функциональность

### Unity WebGL интеграция
- 3D персонаж с Toon Shader в центре экрана
- Анимации, синхронизированные с этапами диалога:
  - **Idle** - покой (начальное состояние)
  - **Listening** - прослушивание (во время записи)
  - **Thinking** - размышление (обработка запроса)
  - **Talking** - разговор (воспроизведение ответа)
- CSS анимация подсветки вокруг персонажа
- Асинхронная загрузка Unity сцены

### Запись аудио
- Использует `arecord` (ALSA) для захвата аудио
- Формат: 16 kHz, mono, 16-bit PCM
- Работает без бинарных зависимостей

### Распознавание речи (STT)
- Поддержка OpenAI Whisper API
- Возможность интеграции с Yandex SpeechKit
- Заглушка для тестирования без API

### Генерация ответов (LLM)
- Поддержка OpenAI GPT
- Возможность интеграции с DeepSeek, YandexGPT
- Настраиваемая модель и параметры

### Синтез речи (TTS)
- Поддержка Yandex TTS API
- Возможность интеграции с Silero TTS
- Воспроизведение через `aplay` (ALSA)

## 🔧 ARM оптимизация

### Особенности
- ✅ Полностью на TypeScript с strict mode
- ✅ Использование только штатных Linux утилит (`arecord`, `aplay`)
- ✅ Отсутствие бинарных NPM модулей
- ✅ Hardware acceleration для Electron
- ✅ Поддержка Wayland и X11
- ✅ Fullscreen + Kiosk режим

### Настройка аудио устройств

```bash
# Просмотр доступных устройств
arecord -l
aplay -l

# Настройка устройства по умолчанию
# Создайте файл ~/.asoundrc или /etc/asound.conf
pcm.!default {
    type hw
    card 0
    device 0
}

ctl.!default {
    type hw
    card 0
}
```

### Настройка udev для микрофона (если нужно)

```bash
# Создайте файл /etc/udev/rules.d/99-microphone.rules
SUBSYSTEM=="sound", KERNEL=="card*", MODE="0666"
```

## 🚀 Автозапуск через systemd

Создайте файл `/etc/systemd/system/voice-assistant.service`:

```ini
[Unit]
Description=Voice Assistant ARM
After=network.target sound.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/voice-assistant-arm
Environment="DISPLAY=:0"
Environment="XAUTHORITY=/home/pi/.Xauthority"
ExecStart=/usr/bin/npm run electron
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активация:

```bash
sudo systemctl enable voice-assistant.service
sudo systemctl start voice-assistant.service
```

## 📦 Сборка дистрибутива

### AppImage для ARM64

```bash
npm run dist:arm64
```

Результат будет в папке `release/`.

### Установка зависимостей для electron-builder

```bash
npm run rebuild
```

## 🐛 Решение проблем

### Проблема: Unity не загружается

```bash
# Проверьте наличие файлов Unity билда
ls -la unity/Build/

# Проверьте консоль Electron (DevTools) на ошибки загрузки
# Убедитесь, что пути к файлам правильные в unity-loader.ts
```

### Проблема: Анимации Unity не работают

- Убедитесь, что в Unity сцене есть GameObject с именем `CharacterController`
- Проверьте, что методы `PlayIdle`, `PlayListening`, `PlayThinking`, `PlayTalking` публичные
- Проверьте настройки Animator Controller в Unity

### Проблема: arecord не находит микрофон

```bash
# Проверьте доступные устройства
arecord -l

# Проверьте права доступа
sudo usermod -a -G audio $USER
# Перелогиньтесь после этого

# Проверьте уровень громкости
alsamixer
```

### Проблема: Electron не запускается

```bash
# Установите зависимости для Electron
npm run rebuild

# Проверьте архитектуру
uname -m  # Должно быть aarch64 или arm64
```

### Проблема: Нет звука

```bash
# Проверьте устройство воспроизведения
aplay /usr/share/sounds/alsa/Front_Left.wav

# Настройте устройство по умолчанию
alsamixer
```

### Проблема: Низкая производительность Unity

- Уменьшите разрешение рендеринга в Unity
- Оптимизируйте количество полигонов персонажа
- Используйте более простые шейдеры
- Включите GPU acceleration в Electron

## 📝 Лицензия

MIT

## 🤝 Вклад

Приветствуются pull requests и issues!

## 📞 Поддержка

Для вопросов и проблем создавайте issues в репозитории проекта.

---

**Версия**: 1.0.0  
**Платформа**: Linux ARM64  
**Совместимость**: BTT Pi 1.2, Orange Pi, Raspberry Pi и другие ARM устройства

