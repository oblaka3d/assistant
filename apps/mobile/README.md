# Mobile App (Placeholder)

Папка предназначена для будущего React Native / Expo приложения.

Пока здесь только `package.json` с заглушками. Когда начнём мобильную разработку:

1. Инициализируйте проект (`npx create-expo-app` или `npx react-native init`) **в этой папке**.
2. Обновите скрипты `dev` / `start` / `build`.
3. Подключите общие пакеты из `packages/shared`.
